# 鲜知 FreshKeeper · 设计说明

面向家庭的食材保质期决策应用。用户录入食材名称、购买日期、包装状态、保存位置；
系统按食材分类的建议期限计算**优先处理顺序**，在临近期限时给出
**“尽快食用 / 适合冷冻 / 建议丢弃”** 等明确建议，并记录开封、移位、做熟、
重新加热等会改变期限的事件。选择若干食材后，系统按现有库存组合简单处理方案，
方案显式注明使用了哪些食材、做完后哪些仍需尽快处理；所有记录可修改、可追溯。

本仓库是可直接运行的参考实现：纯前端、零后端、localStorage 本地持久化。

## 1. 需求到功能的对应关系

| 需求 | 实现位置 |
| --- | --- |
| 录入名称、购买日期、包装状态、保存位置 | 录入弹层（`index.html` sheetForm / `app.js openForm`） |
| 不同食材建议期限 | 规则库 `js/rules.js`（20 个分类 × 3 种位置 × 密封/开封） |
| 计算优先处理顺序 | `engine.assessAll()`：五档状态 + 优先级分数排序 |
| 拍照后手动确认识别文字 | `js/ocr.js`（Tesseract 中英文识别 + 标签解析，仅回填表单，必须人工确认才入库） |
| 尽快食用/适合冷冻/建议丢弃 | `engine.buildAdvice()` 输出标题、说明、备选动作；planner 产出处置方案 |
| 记录开封/重新加热等改变期限的情况 | 事件时间轴：`open / move / freeze / thaw / cook / reheat / consume / discard` |
| 选择几样食材组合简单方案 | 方案页快速勾选；`planner.buildPlans(scopeItems)` |
| 注明使用了哪些食材 | 方案 `used[]`，卡片与详情都展示 |
| 注明哪些仍需尽快处理 | 方案 `stillUrgent[]`（应用方案后未覆盖的临期/过期库存） |
| 手动添加待购/从吃完或丢弃食材补货 | 补货视图与已归档详情「再买一份」；`storage.addShopping(fields, source)` |
| 常备食材预警：设常备数量，低于自动生成待购（含建议购买量），买到录入后自动解除 | 补货视图常备块 + 弹层（`sheetStaple`）；存储层 `staples` 集合与 `syncStaplesImpl()` 随库存变动重估；`staple.add/update/remove/alert/resolve` 全入 `audit` |
| 添加时提示同名在库食材 | `sameNameStock()` 归一化名称比对，勾选“仍要购买”才能保存 |
| 家庭共享采购：负责人、备注、待认领/已认领/已购买 | 待购表单可选负责人（留空=待认领）；`claimShopping/transferShopping/releaseShopping`；状态筛选 |
| 购买后录入、保存成功才完成待购并关联新库存 | `openForm(null, {purchaseShopId})` → 保存时 `completeShopping(id, newItemId)`；取消则待购保留 |
| 从库存/方案选食材建用餐计划，定日期与名称 | 计划视图新建弹层（库存勾选清单）；方案卡「📅 加入计划」一键带入 |
| 建立后可改用餐日期、名称和食材，改完仍是同一条计划 | 计划卡「编辑」复用同一弹层预填；`storage.updateMealPlan(id, fields)` 仅待用餐可改，字段级 from → to 入 `audit`（`mealplan.update`），id/createdAt 不变 |
| 展示食材在计划日的预计状态，临期/过期给调整提示 | 引擎 `assess(item, 计划日)` 快进评估 + `planAdjustment()` 纯函数；表单预览与计划卡片同步展示 |
| 计划清单按日期排列，可标记完成、跳转食材详情 | `storage.listMealPlans()`（待用餐按日期升序）；计划卡食材标签点击进详情 |
| 完成时沿用做熟/吃完/丢弃事件记录 | `completeMealPlan(id, actions)` 逐样调用现有 `addEvent`（source 记 `mealplan:<id>`，可撤销） |
| 完成后自动更新计划状态与审计流水 | 计划置 `done/doneAt`；`mealplan.add/complete/remove` 与每条事件均入 `audit` |
| 为成员记录忌口、过敏食材分类和偏好 | 成员弹层（`index.html` sheetMembers/sheetMemberForm）；`storage.addMember/updateMember`；标签 = `cat:<id>` 分类或自定义关键词 |
| 创建方案/用餐计划时选择就餐成员 | 方案页 `#planDiners` 与计划表单 `#mDinerBar` 成员条；选择记忆在本机 `freshkeeper:diners` |
| 对所选库存和方案食材给出明确冲突提示 | `diet.evaluate(members, targets)` 返回 blockers/warnings/likes；方案卡、快速勾选区、计划表单预览、计划卡、食材详情同步标注 |
| 确认后继续或替换冲突食材 | 冲突解决弹层（sheetDiet）：方案可**同槽位替换**（`planner.substitutionCandidates/substituteInPlan`）、计划可移除；过敏必须勾选知悉风险才能继续，确认快照入审计 |
| 所有记录可修改、可追溯 | 字段修订 `revisions[]`、事件软撤销、审计流水 `audit[]`、软删除可恢复、待购与用餐计划及常备预警的增删改/完成/触发/解除均入流水 |

## 2. 保质期模型（核心设计）

### 2.1 两种期限

- **安全期限（safeEnd）**：到点即不应再吃的硬边界。过期 → “建议丢弃”。
- **品质期限（qualityEnd）**：超过后口感/营养明显下降但未必不安全。
  冷冻室主要使用品质期限（生肉约 6 个月、熟制品约 2–3 个月）。

### 2.2 冷冻“时钟暂停”模型

- 放入冷冻时，记录此刻剩余的安全天数余额 `remainingDays`，安全时钟暂停。
- 冷冻期间只按品质期限评估（`fresh / quality`，永远不会在冷冻中被判“过期”）。
- 解冻移冷藏时恢复时钟：`新期限 = min(该位置常规期限, 冷冻前剩余余额, 分类解冻期限)`，
  并标记 `thawed`，此后不再建议“再次冷冻”（方案与建议中会过滤掉冷冻选项）。
- 购入即在冷冻室的，按分类解冻规则（多数 1–2 天）赋予解冻余额。

### 2.3 改变期限的事件（事件溯源）

每条食材记录 = **当前字段 + 一串不可变事件**。所有结论由 `rebuild()` 重放得到：

| 事件 | 对期限的影响 |
| --- | --- |
| `open` 开封 | 期限截断为“已开封”档（如酸奶 21 天 → 3 天） |
| `move` 移位 | 按新位置规则重算，携带剩余余额 |
| `freeze` 冷冻 | 安全时钟暂停，开始计品质期 |
| `thaw` 解冻 | 恢复时钟，按解冻期限，标记不可再冻 |
| `cook` 做熟 | 改按“熟菜冷藏 1–3 天”规则 |
| `reheat` 复热 | **不重置**期限，仅提示“当餐吃完、勿二次加热” |
| `consume/discard` | 归档，离开处理队列 |

事件可以**撤销**（软删除标记，历史保留），撤销后引擎重放自然回到事件前结论。
这让“改错了/误操作”总能回到任意历史状态，而不需要备份多份数据。

**同一天多事件的次序**：每个事件带食材内单调递增的 `seq`（存储层 `addEvent` 分配）。
引擎提供唯一的比较器 `compareEventsAsc/Desc`，排序键依次为
事件日期 → `seq` → `createdAt` 实际时间戳 → `id`（全等返回 0，保证稳定）。
重放用升序、详情时间线用倒序，二者共用同一比较器——避免“同一天先冷冻又解冻”
被按错误顺序重放，或时间线在同日事件上返回不确定次序（旧实现相等时恒返回 -1）。
历史数据缺少 `seq` 时，导入/加载归一化按文件中的先后次序补号并避开已有序号。

### 2.4 分档与建议（高风险从严）

高风险分类：生鲜肉、禽肉、水产、熟肉、剩菜、熟主食、液态奶、酸奶、豆制品、速冻食品。

| 档位 | 高风险食材 | 普通食材 | 主建议 |
| --- | --- | --- | --- |
| expired | 已超安全期限 | 同左 | 建议丢弃（高风险明确“无异味也别吃”） |
| danger | 剩余 ≤ 1 天 | 到期当天 | 立即吃；可冷冻的立即分装冷冻 |
| warn | 剩余 ≤ 2 天 | 剩余 ≤ 3 天 | 尽快食用；吃不完可冷冻 |
| quality | 超过品质期但安全期限内 | 同左 | 可吃，建议重调味烹饪 |
| fresh | 其余 | 其余 | 正常安排 |

优先级分数：`expired(1000) > danger(800) > warn(500) > quality(200) > fresh(50)`，
再叠加剩余天数紧迫度和高风险加权，库存列表即按该分数降序排列。

### 2.5 散装的特殊处理

生鲜蔬果（叶菜/瓜果茄/根茎/菌菇/水果）“散装称重”在家庭语义里等同于整颗未处理，
因此按密封档期限计；而乳品、熟食、豆制品等加工即食食品散装视同开封。
见 `engine.js` 的 `LOOSE_AS_SEALED`。

### 2.6 不建议的保存位置

规则中某位置天数为 `null`（如带壳鸡蛋冷冻、水产常温）时：
界面给出明确警告，引擎按最保守 1 天兜底，避免给出虚假的安全感。

## 3. 组合方案设计

两类方案，统一结构（`planner.js`）：

1. **烹饪方案（cook）**：8 个家常模板（快炒、白灼、蔬菜豆腐汤、番茄炒蛋、
   什锦炒饭、炖菜、酸奶杯、速冻水饺汤餐）。模板由“食材槽位”组成，
   每个槽位可由一组分类中的任一种填充；必需槽位不齐时**不出方案、不硬凑**。
   同一食材不会被一个方案重复使用；优先消耗最紧迫的食材（贪婪打分）。
2. **处置方案**：
   - `discard`：把所有过期食材打包成一次“清理”任务；
   - `freeze`：临期且适合冷冻、且未解冻过的食材 → 分装冷冻操作指引；
   - `reheat`：已做熟的剩菜/主食临期 → 彻底复热、当餐吃完。

每个方案对象都带：

- `used[]`：使用了哪些库存食材（含“今天到期/冷冻中需解冻”等标注）；
- `steps[]`：操作步骤（用到冷冻食材时自动前置解冻步骤）；
- `stillUrgent[]`：**应用方案后仍处于过期/临期/尽快状态的其他食材**——
  这是题目明确要求的“注明哪些仍需尽快处理”；
- `eventsOnApply[]`：点“按此方案处理并记录”时批量写入的期限事件，
  随后方案结果会真实反映到库存和审计中。

## 4. 拍照 OCR 流程（人始终在环内）

拍照/相册 → Tesseract.js（CDN 懒加载，`chi_sim+eng`）→ `parseLabel()` 纯文本解析：

- 提取生产日期/保质期天数/“保质期至”日期，推算或直接取到期日；
- 识别贮存条件关键词（-18℃ 冷冻 / 0–4℃ 冷藏 / 常温阴凉）；
- 猜测品名（过滤净含量、配料表、厂商等停用行）。

解析结果**只回填录入表单**（到期日写入备注提示用户核对购买日期），
页面明确提示“识别结果不会自动入库”，用户逐项确认后才保存。
离线或 CDN 不可用时降级为纯手动输入。

## 5. 数据与追溯

localStorage 单键 `freshkeeper:v1`：

```jsonc
{
  "items": [
    {
      "id", "name", "categoryId", "purchaseDate", "packageType", "location", "note",
      "events": [ { "id", "type", "at", "source", "deleted", "deletedAt" } ],
      "revisions": [ { "at", "fields": {旧字段快照} } ],
      "createdAt", "removed", "removedAt"
    }
  ],
  "shopping": [
    { "id", "name", "categoryId", "qty", "note", "status",
      "assignee", "claimedAt", "source", "sourceItemId", "stapleId",
      "createdAt", "completedAt", "itemId" }
  ],
  "mealPlans": [
    { "id", "name", "date",
      "items": [ { "id", "name" } ],
      "members": [ { "id", "name" } ],   // 创建时的就餐成员快照
      "diet": { "blockers": [...], "warnings": [...], "acknowledgedAt" }, // “明知冲突仍继续”的确认快照（无冲突则无此字段）
      "status", "source", "createdAt", "doneAt" }
  ],
  "members": [
    { "id", "name", "note",
      "allergyTags": [...],   // 过敏：cat:<categoryId> 分类标签或自定义食材关键词（如“花生”）
      "avoidTags":   [...],   // 忌口/不喜欢（标签形态同上）
      "preferTags":  [...],   // 偏好/爱吃
      "createdAt" }
  ],
  "staples": [
    { "id", "name", "categoryId",
      "minQty",                // 常备数量：在库低于该数触发预警（1–99 整数）
      "note", "createdAt" }
  ],
  "audit": [ { "seq", "at", "action", "detail", "snapshot" } ]
}
```

- 字段修改：旧值整体快照进 `revisions`，`audit` 记录每个字段 from → to；
- 事件：全部进 `audit`，撤销是标记删除而非物理删除；
- 删除食材：软删除，追溯页可一键恢复；
- 审计流水以**实际发生时间 `at` 为主序**（倒序，最新在前），单调 `seq` 仅在
  同一毫秒内作为次序兜底。合并外部备份时**不平移外部 seq**——否则一份较早生成的
  备份（seq 可能很大）会被整体排到本地最新操作之后；导入后 `auditSeq` 取本地与
  外部序号的最大值，保证后续新事件序号继续单调；
- 设置里可导出/合并导入 JSON、清空数据。

### 5.0 写入失败（容量写满 / 存储不可写）：事务化落盘 + 分类提示 + 历史瘦身

手机浏览器对单源 localStorage 容量限制很小（常见 5 MB），而“追溯流水永不物理删除”
会让数据单调增长，写满后 `setItem` 抛 `QuotaExceededError`；另一类失败是无痕/隐私
窗口或浏览器设置禁止存储，常抛 `SecurityError`（旧 iOS Safari 隐私窗口也可能抛
`QuotaExceededError`）。旧实现把异常直接抛进事件处理器（界面毫无反应），且修改已经
进了内存——用户看到“已保存”，刷新即丢。

三层对策：

1. **存储层事务（原子性）**：每个对外变更操作（addItem / addEvent / applyPlan /
   completeMealPlan / importJSON… 共 22 个）都包在 `tx()` 中。事务内 `persist()`
   不落盘，操作成功结束后一次性 `setItem`；
   - 落盘成功 → 整笔操作生效，推进检查点；
   - 落盘失败（配额写满，或隐私模式等其它写异常，由 `isQuotaError` 判定）→
     内存整体回滚到事务开始前的快照，抛出 `StorageWriteError`，其 `quota` 标记
     区分两种成因，磁盘停留在操作前状态。内存/磁盘严格一致，杜绝半成品
     （如多事件方案只写进一半）。
2. **界面按成因给两套不同引导**（`showStorageFull(err)`）。所有保存入口经
   `guard()` 调用存储层，捕获后弹出**不自动消失**的失败弹层，表单/弹层保持打开、
   输入不丢；另有 `window.onerror` 兜底，防止漏网的写异常静默。
   - **`quota=true` 容量写满**：标题「本机存储空间已满」，展示各部分占用
     （`storageInfo()`），步骤为 ①先导出备份 ②**清理历史痕迹**（清完通常即可继续）
     ③仍失败则导出后清空再导回；显示「清理历史痕迹」按钮。
   - **`quota=false` 存储不可写**（无痕/隐私窗口、存储被禁）：标题「浏览器存储
     不可写」，**不显示占用分布与“清理历史痕迹”按钮**（删除数据解决不了不可写，
     引导错了反而让用户白删追溯），步骤改为 ①先导出备份 ②**退出无痕/隐私窗口、
     用普通窗口重试** ③仍不行则**更换浏览器**或检查本地存储设置。
3. **历史瘦身 `pruneHistory({ keepAudit, keepRevisions })`**：只删冗余痕迹——
   audit 保留最近 500 条（仍在回收站食材的 `item.remove` 条目保留，那是恢复入口，
   超窗保留的条目剥掉大快照）、每样食材 `revisions` 保留最近 20 份；
   食材、期限事件、撤销标记、待购、计划、成员一律不动。瘦身用 `forcePersist()`
   先落盘（数据只会变小，不能被随后那笔 prune 流水的失败连带回滚），再补记
   `history.prune` 流水；瘦身后仍写不进则继续报错，引导导出备份后清空重建。
   设置弹层里也常驻同一入口，可在写满前主动瘦身。

### 5.1 导入的结构校验（防止畸形数据入库导致渲染崩溃）

导入与本地加载采用**两种严格度不同的策略**：

- **导入（严格，原子拒绝）**：写入前用 `validatePayload()` 校验整份文件——
  顶层必须是含 `items` 数组的对象；每条食材必须有非空名称、合法 `YYYY-MM-DD`
  购买日期、合法位置（冷藏/冷冻/常温）与包装（密封/开封/散装）；
  事件必须是数组，且每个事件类型受支持、带合法日期。任意一条记录不合法都
  **整体抛错、不写入任何数据**，已有库存（含覆盖模式）完全不变，界面弹框列出
  具体原因。同一文件内或与现有库存重复的 ID 也拒绝合并，避免静默串记录。
  缺少 `id`、畸形但可推断的可选字段会被补全/归一化后才入库。
- **日期按真实日历校验**：所有 `YYYY-MM-DD` 校验（`isDateStr`）都会识破
  2月30日、4月31日、平年2月29日 这类“格式对但不存在”的日期——JS Date 构造
  会把它们溢出成下个月某天，若放行，引擎会按溢出后的另一天计算预计状态，
  与用户填的日期对不上。因此：新建/编辑（表单与存储层双重拦截）与严格导入
  一律拒绝；`Engine.parseISODate` 对不存在的日期返回 `null` 而不是溢出后的日期。
- **本地加载（宽松迁移）**：旧版本或手工改过 localStorage 造成的脏数据，
  核心字段（名称、购买日期）合法就保留，非法位置/包装兜底为默认值，
  `null`/非数组的 `events`、`revisions` 归一化为空数组，畸形事件剔除，
  无法修复的记录跳过并告警，归一化结果回写，保证页面永远不因历史数据白屏。
  其中“格式对但不存在”的日期（购买日期/事件日期/用餐日期）**改正为当月
  最后一天**（2026-02-30 → 2026-02-28）以保住记录；月份越界等无法合理改正的
  才按原规则跳过。

### 5.2 补货（家庭共享采购）模型

`shopping[]` 与 `items[]`、`audit[]` 同存于一个 localStorage 键，
随导出/导入/清空一并处理，导入时与食材记录一样走严格结构校验（畸形整体拒绝）：

```jsonc
{
  "id", "name", "categoryId", "qty", "note",
  "status": "unclaimed" | "claimed" | "done",   // 待认领 / 已认领 / 已购买
  "assignee", "claimedAt",                       // 负责人（≤20 字）与最近一次认领/转交时间
  "source": "manual" | "consume" | "discard" | "staple",  // 手动 / 吃完补货 / 丢弃补货 / 常备预警自动生成
  "sourceItemId", "sourceName",                 // 发起补货的归档食材
  "stapleId",                                    // source=staple 时关联的常备预警
  "createdAt", "completedAt",
  "itemId"                                      // 完成后关联的新库存 ID
}
```

三态协作流（待认领 → 已认领 → 已购买）：

- 添加时**负责人留空即“待认领”**，填写负责人则直接进入“已认领”（`claimedAt` 取创建时间）。
- **认领** `claimShopping(id, assignee)`：待认领项记录负责人 → 已认领；
  对已被别人认领的项再次认领，按**转交**语义处理（`from → to` 都写入流水），
  适配“谁顺路谁顺手拿”的家庭场景，不做锁。
- **转交** `transferShopping(id, assignee)`：更换负责人，状态保持已认领；
  空名字/与当前负责人相同返回 `false`，不产生流水。
- **取消认领** `releaseShopping(id)`：已认领 → 待认领，清空 `assignee/claimedAt`；
  卡片重新回到待认领筛选与排序靠前的位置。
- 编辑表单也可修改负责人，存储层按同一口径落成 claim/transfer/release 流水；
  已购买项的负责人不再被编辑改动（作为购买时的归属快照保留）。
- “我是谁”：补货视图顶部身份条把当前家庭成员称呼存在 localStorage
  （`freshkeeper:member`，与业务数据分开存键，但**清空全部数据时会一并删除**），
  认领弹层默认带入；历史负责人收集为 `listShopMembers()` 供 datalist 与成员快选 chip 使用。
  应用本身无账号体系，身份只是本机便利输入，不参与冲突判定。

关键交互约束（刻意不做“一键入库”）：

- 添加时实时按归一化名称（去空白、转小写，与分类识别同一 `normalizeName`）
  匹配**未归档**的同名在库食材；命中即在表单内列出其状态与剩余天数，
  用户必须勾选“确认库存已有，仍要加入待购”才能保存，避免重复购买。
  勾选状态在输入重渲染间保留（修改名称不丢失已确认状态）；
  **编辑**既有待购项且名称未改时不再重复拦截——加入时已确认过，
  避免“改个数量也要再次勾选”导致流程卡住；改名成另一个同名在库食材时才重新提示。
- 点「已买到」**不直接改状态**，而是打开现有录入表单并预填名称与分类
  （待认领/已认领项都可购买，其余购买日期/位置/包装仍按新食材录入，由用户确认）。
  只有表单校验通过、`addItem` 成功后才调用 `completeShopping(id, newItemId)`
  写入 `done/completedAt/itemId`（购买时的负责人随快照保留）；取消录入或校验失败时
  待购项原样保留，下次仍可继续购买。录入事件的 `source` 记为 `restock:<shoppingId>` 可追溯。
- 已吃完/已丢弃食材详情页提供「再买一份」，来源事件（`consume`/`discard`）
  与来源食材 ID 一并记录，待购卡片显示“吃完补货/丢弃补货”标记。
- 补货列表按状态筛选：**待办（待认领+已认领，默认）/ 待认领 / 已认领 / 已买到**；
  排序为待认领 → 已认领 → 已购买，同组内新的在前，已购买按完成时间倒序。
  底部导航角标实时显示**待办总数**（未购买的全部，含已认领）。
- 已买到列表可跳转到关联的新库存详情；关联库存事后被删除时显示占位文案而非报错。
- 待购的添加/编辑/认领/转交/取消认领/完成/删除全部进入 `audit`
  （`shopping.add/update/claim/transfer/release/complete/remove`），
  认领类流水记 `from → to` 负责人，编辑流水记字段级 from → to。

### 5.3 用餐计划模型（与库存直接衔接的闭环）

`mealPlans[]` 与 `items[]`、`shopping[]`、`audit[]` 同存于一个 localStorage 键，
随导出/导入/清空一并处理，导入时走同样的严格结构校验（畸形整体拒绝）：

```jsonc
{
  "id", "name",            // 计划名称（用户指定，方案带入时默认为方案名）
  "date",                  // 用餐日期 YYYY-MM-DD
  "items": [ { "id", "name" } ],  // 创建时的食材快照：id 用于跳转详情，name 保证归档/改名后仍可展示
  "status": "pending" | "done",
  "source": "manual" | "plan",    // 手动新建 / 方案页一键带入
  "createdAt", "doneAt"
}
```

闭环的四个环节：

1. **选择食材与日期**：新建弹层列出全部在库食材（按处理紧迫度排序）供勾选；
   方案页的烹饪方案可一键带入（方案名 → 计划名，方案食材 → 预勾选）。
2. **计划日预计状态**：引擎 `assess(item, 计划日)` 把每样食材“快进”到用餐日评估，
   纯函数 `planAdjustment()` 把评估结果翻译成调整提示——
   已过期（建议提前食用/改早计划/适合冷冻的分装冷冻）、临期、临近建议期限、
   仍在冷冻中（需提前解冻）、已归档（需更换食材）。
   表单内实时预览，确认后计划卡片上继续按同一口径展示。
3. **按日期排列的计划清单**：待用餐按用餐日期升序（最近的在前，并标注
   今天/明天/N 天后/已过期 N 天），已完成按完成时间倒序；
   食材标签可点击跳转食材详情，底部导航角标显示待用餐数量。
4. **建立后可编辑**：待用餐计划可改名称、用餐日期、食材与就餐成员——
   `updateMealPlan(id, fields)` 只改字段不重建议（id、createdAt 不变），
   非法输入（空名称/坏日期/空食材）整体拒绝、原计划不变；
   用餐日期按真实日历校验，2月30日 这类不存在的日期在新建/编辑/导入时一律拒绝；
   编辑弹层与新建共用，计划里已归档/已删除的食材追加为可取消勾选的行，
   避免保存时被静默丢掉；冲突确认快照随编辑更新（无冲突则清除）；
   字段级 from → to 写入 `mealplan.update` 流水，无实际变更不写流水。
   已完成的计划是历史记录，不可再改。
5. **完成即事件记录**：`completeMealPlan(id, actions)` 对每样食材沿用现有
   `addEvent` 写入 做熟/吃完/丢弃（`source` 记 `mealplan:<planId>`，
   详情时间线标注“来自用餐计划”且可撤销），已删除/已归档食材自动跳过；
   计划置 `done/doneAt`，`mealplan.add/update/complete/remove` 与每条事件全部入 `audit`。

### 5.4 家庭成员饮食偏好与忌口/过敏模型

`members[]` 与其他业务数据同存于一个 localStorage 键，随导出/导入/清空一并处理，
导入走严格结构校验（姓名必填、三类标签必须是数组，畸形成员整体拒绝）；
本地加载旧数据时宽松迁移（畸形标签置空、坏成员跳过）。成员姓名按归一化值
（去空白、转小写）唯一，重复添加拒绝；删除成员不级联历史计划，只保留姓名快照。

每个成员三组标签（空数组/缺字段 = 无限制，全模块对空值容错、永不抛错）：

- `allergyTags` 过敏：命中即**阻断级**冲突，不勾选“知悉风险”不能继续；
- `avoidTags` 忌口/不喜欢：命中为**警告级**，提示后可直接继续；
- `preferTags` 偏好/爱吃：只做正向提示，永不阻断。

标签两种形态（纯逻辑在 `js/diet.js`）：

- `cat:<categoryId>` 分类标签（如 `cat:seafood`）：按食材 `categoryId` 命中，
  食材只给名称时由引擎自动识别分类兜底；
- 自由文本（如“花生”“香菜”）：按名称归一化（去空白转小写）后的**包含**匹配，
  因此“花生酱”也会命中“花生”。

匹配与分级全部由纯函数 `Diet.evaluate(members, targets)` 完成，返回
`{ blockers, warnings, likes, blockerCount, … }`，目标可传库存 item、
引擎评估结果或 `{id,name}`。界面在五处消费同一结果：方案库存勾选区（chip 标 🚫/⚠️）、
方案卡与方案详情（冲突框 + 食材标红/标橙 + 偏好提示）、用餐计划食材清单行高亮、
计划表单实时预览、食材详情“家里谁需要避开”。

冲突解决弹层（`sheetDiet`）两种模式：

- **方案（plan）**：每行可点「替换」，`planner.substitutionCandidates()` 在
  **同一菜谱槽位**（同分类组）的其他在库食材中挑选：未过期、未归档、排除方案已用、
  排除被替换者自身；排序为 过敏安全 → 偏好人数 → 紧迫度 → 名称。
  `substituteInPlan()` 返回**替换后的新方案对象**（不改原方案），steps、
  eventsOnApply、stillUrgent 全部按同一套装配逻辑重算；没有同类食材时明确提示
  “无候选，可确认风险继续”。
- **用餐计划（meal）**：每行可点「移除」直接取消勾选；移除后实时重评，
  冲突清零则自动继续创建，食材清空则回到表单。

过敏确认的交互约束（与待购同名确认同一风格）：有阻断行时「确认并继续」按钮
必须等用户勾选知悉风险才放行；确认时的食材+成员+理由快照随 `plan.apply`
（`detail.dietAck/memberNames`）或 `mealplan.add`（计划 `diet` 字段与审计
`detail.dietAck`）记录在案，计划卡片长期展示“已确认过敏风险/忌口”。
方案页与计划表单共用一份本机就餐成员记忆（`freshkeeper:diners`，清空数据时删除）。

### 5.5 常备食材预警模型（低于常备量 → 自动待购 → 买到解除）

`staples[]` 与其他业务数据同存于一个 localStorage 键，随导出/导入/清空一并处理，
导入走严格结构校验（名称必填、`minQty` 必须是 1–99 的整数，畸形整体拒绝）；
本地加载旧数据时宽松迁移（缺数量补 1、超界截断、坏记录跳过）。无 `staples` 字段的
旧版本数据加载为空数组。常备名称按归一化值（去空白、转小写）唯一，重复添加拒绝。

```jsonc
{
  "id", "name", "categoryId",
  "minQty",      // 常备数量：在库低于该数触发预警
  "note", "createdAt"
}
```

**在库数量** = 未删除、未归档（无 consume/discard 终止事件）的同名食材条数，
名称比对与待购同名确认同一 `normalizeName` 口径（去空白、转小写、精确匹配）；
冷冻中的食材计入在库。

**重估时机**：预警不存状态，每次库存变动后由存储层 `syncStaplesImpl()` 全量重估
（addItem / updateItem / addEvent / undoEvent / removeItem / restoreItem /
completeShopping / 导入 / 常备设置增删改，全部在同一写入事务内完成，失败整体回滚）：

- **触发**：在库 < minQty 且没有开口待购覆盖 → 自动生成
  `source: 'staple'`、`stapleId` 关联的待认领待购项，`qty` = **建议购买量**
  （minQty − 在库，文案 “N 份”），写 `staple.alert` 流水（含在库数/常备数/建议量/待购 ID）。
  已有同名开口待购（手动或吃完/丢弃补货）视为已覆盖，**不重复生成**；
  手动/补货来源的待购项数量是用户填的，系统不改写。
- **重算**：本预警的自动待购项的 `qty` 不是生成时的一次性快照——每次重估都按
  **当前缺口**（minQty − 在库）刷新，数量变化写 `staple.alert.update` 流水
  （from → to 与当时在库数）。否则在库回升后待购项仍写旧数量，用户照着买就买多了。
  重算只改数量，认领状态与负责人不受影响。
- **解除**：在库回升到常备线（买到录入、撤销归档、恢复删除等任意路径）→
  自动生成的开口待购项被撤下，写 `staple.resolve` 流水。走「已买到」流程时，
  `addItem` 的重估先于 `completeShopping`：买够了 → 待购项撤下、完成幂等返回
  false（界面按“预警自动解除”提示）；**没买够** → 旧项正常完成，并立即按新的
  缺口重新生成一条预警待购。
- **删除常备设置**：一并撤下其自动生成且仍待认领的待购项（已认领/已购买的保留，
  不打扰进行中的采购），写 `staple.remove` 流水（含撤下条数）。
- 用户手动删除自动待购项后，若库存仍低于常备线，下次库存变动会重新生成——
  提醒按设计是“执着”的；不再需要提醒时应删除常备设置本身。

界面入口在补货视图顶部的常备块：每条常备显示 常备数量 / 在库数量 / 充足|需补货 /
建议购买量 / 是否已生成待购项，可编辑、可删除；自动生成的待购项在清单中带
“🔔 常备预警”来源标记，购买流程与普通待购完全一致（保存成功才完成并关联新库存）。
`staple.add/update/remove/alert/alert.update/resolve` 全部进入 `audit` 追溯。

## 6. 目录与测试

```
freshkeeper/
├── index.html            # 单页应用（库存/补货/计划/方案/追溯五个视图 + 弹层）
├── css/styles.css        # 手机优先样式
├── js/
│   ├── rules.js          # 分类规则库（决策依据的数据层）
│   ├── engine.js         # 纯逻辑：分类、期限重放、分档、建议、优先级、计划日调整提示
│   ├── diet.js           # 纯逻辑：饮食标签匹配、过敏/忌口/偏好冲突分级、替换安全性
│   ├── planner.js        # 组合方案（菜谱槽位匹配 + 处置方案 + 冲突食材同槽位替换）
│   ├── ocr.js            # 拍照识别 + 标签文本解析（解析函数可单测）
│   ├── storage.js        # 持久化 + 修订 + 审计流水 + 待购 + 用餐计划 + 家庭成员 + 常备预警
│   └── app.js            # DOM 渲染与交互（不含业务规则）
└── test/
    ├── engine.test.js    # node:test 单元测试
    ├── import.test.js    # 导入校验 / 宽松迁移单元测试
    ├── shopping.test.js  # 家庭采购单元测试（三态/认领/转交/取消/迁移）
    ├── mealplan.test.js  # 用餐计划闭环单元测试
    ├── diet.test.js      # 饮食偏好/忌口/过敏单元测试（匹配/分级/替换/成员 CRUD/导入）
    ├── staple.test.js    # 常备预警单元测试（触发/建议量/解除/CRUD/导入迁移）
    └── e2e.smoke.js      # jsdom 端到端界面断言
```

运行：

```bash
npm test            # 单元测试（node --test，零依赖）
npm run test:e2e    # 端到端冒烟（需 jsdom）
npm start           # http://localhost:8080 静态服务，手机同局域网可访问
# 或直接用浏览器打开 index.html
```

## 7. 已知边界与刻意取舍

- 规则数值是家庭保守建议，不替代包装标注；OCR 识别到包装期限时提示用户以包装为准。
- “做熟”统一按食材分类的熟菜安全期限（1–3 天），不区分具体菜品。
- 分类靠名称关键词匹配（最长优先），识别不到时要求手选分类并按 1 天兜底，宁严勿松。
- Tesseract 模型从 CDN 懒加载；离线环境拍照识别不可用，手动录入不受影响。
- 数据仅存本机浏览器；可通过导出 JSON 自行备份/迁移。
